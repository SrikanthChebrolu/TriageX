[
  {
    "id": "INC-001",
    "title": "Market data feed stale prices — all instruments",
    "description": "Feed handler lost upstream provider connectivity. Price engine detected staleness threshold exceeded on all instruments. Orders being rejected due to stale quotes.",
    "severity": "CRITICAL",
    "affectedServices": ["market-data-feed", "price-engine", "order-gateway"],
    "rootCause": "Upstream feed provider TCP connection dropped silently; heartbeat timeout not configured correctly.",
    "resolution": "Restarted market-data-feed handler and configured TCP keepalive; added provider health check alert.",
    "resolvedInMin": 18,
    "tags": ["market-data", "connectivity", "stale-prices", "order-rejection"]
  },
  {
    "id": "INC-002",
    "title": "Market data feed primary/backup failover failure",
    "description": "Primary feed provider connection dropped and failover to backup provider did not trigger automatically. Automatic failover was disabled by a config change 3 days prior. All equity instruments reported no-quote for 11 minutes.",
    "severity": "CRITICAL",
    "affectedServices": ["market-data-feed", "price-engine", "order-gateway", "risk-engine"],
    "rootCause": "Backup feed failover flag was set to false during a config cleanup; the change was not reviewed against runbook requirements.",
    "resolution": "Re-enabled failover in config; manually switched to backup feed; added CI check to prevent disabling failover in production.",
    "resolvedInMin": 11,
    "tags": ["market-data", "failover", "config", "feed-provider"]
  },
  {
    "id": "INC-003",
    "title": "Market data feed multicast packet loss — derivative instruments",
    "description": "UDP multicast packet loss reached 34% on the derivatives feed channel. Price engine began publishing synthetic mid-prices for affected options contracts. Risk engine flagged elevated VaR due to poor price quality.",
    "severity": "HIGH",
    "affectedServices": ["market-data-feed", "price-engine", "risk-engine"],
    "rootCause": "Network switch firmware upgrade on the multicast VLAN introduced a buffer configuration regression causing packet drops under sustained throughput.",
    "resolution": "Rolled back switch firmware; restored unicast fallback feed for derivatives; added multicast packet-loss monitoring.",
    "resolvedInMin": 42,
    "tags": ["market-data", "multicast", "packet-loss", "derivatives", "network"]
  },
  {
    "id": "INC-004",
    "title": "Market data feed reconnect loop — FX spot prices unavailable",
    "description": "FX feed handler entered a reconnect loop after the provider rotated its TLS certificate. Handler did not trust the new certificate chain and retried every 2 seconds. FX spot prices were unavailable for 27 minutes during European open.",
    "severity": "HIGH",
    "affectedServices": ["market-data-feed", "price-engine", "fix-gateway"],
    "rootCause": "Feed handler's trust store contained an expired intermediate CA certificate; the provider's new cert chain required the updated CA bundle.",
    "resolution": "Updated trust store with current CA bundle; restarted feed handler; automated cert-expiry alerting for trust stores.",
    "resolvedInMin": 27,
    "tags": ["market-data", "tls", "certificate", "fx", "connectivity"]
  },
  {
    "id": "INC-005",
    "title": "Auth service JWT signing key rotation broke all sessions",
    "description": "On-call rotated the JWT signing key without coordinating with downstream consumers. All service-to-service calls returned 401 Unauthorized. Trading halted for 22 minutes.",
    "severity": "CRITICAL",
    "affectedServices": ["auth-service", "trade-executor", "order-gateway", "risk-engine"],
    "rootCause": "JWT signing key rotation invalidated all active tokens; downstream services cached the old public key and had no key-discovery refresh interval.",
    "resolution": "Deployed new public key to all consumers; cleared token caches; added JWKS endpoint with 5-minute TTL for automatic key discovery.",
    "resolvedInMin": 22,
    "tags": ["auth", "jwt", "key-rotation", "401", "cascade"]
  },
  {
    "id": "INC-006",
    "title": "Auth service crash loop — invalid Redis connection string in config",
    "description": "auth-service entered CrashLoopBackOff after a config deployment set an invalid Redis sentinel connection string. All services depending on auth returned 503. Session validation was completely unavailable.",
    "severity": "CRITICAL",
    "affectedServices": ["auth-service", "api-gateway", "trade-executor", "session-manager"],
    "rootCause": "Config deployment introduced a malformed Redis sentinel URL; auth-service failed at startup when it could not connect to the session store.",
    "resolution": "Rolled back config deployment; auth-service recovered; added config schema validation step in the CD pipeline.",
    "resolvedInMin": 10,
    "tags": ["auth", "config", "redis", "crash-loop", "startup-failure"]
  },
  {
    "id": "INC-007",
    "title": "Auth service OAuth2 token endpoint latency spike",
    "description": "Token endpoint p99 latency rose from 80 ms to 4.2 s over 15 minutes. Client applications began hitting 30-second timeouts. Investigation revealed the user DB index had been accidentally dropped during schema migration.",
    "severity": "HIGH",
    "affectedServices": ["auth-service", "user-service", "api-gateway"],
    "rootCause": "Schema migration script dropped the index on the users table email column; subsequent queries performed full table scans.",
    "resolution": "Recreated index online (CONCURRENTLY); latency recovered immediately; added index existence check to migration validation suite.",
    "resolvedInMin": 38,
    "tags": ["auth", "database", "index", "latency", "migration"]
  },
  {
    "id": "INC-008",
    "title": "Session manager token refresh storm after auth-service restart",
    "description": "After auth-service pod restart, all active sessions attempted token refresh simultaneously. session-manager was overwhelmed with 14,000 concurrent refresh requests in under 2 seconds, causing Redis connection exhaustion.",
    "severity": "HIGH",
    "affectedServices": ["auth-service", "session-manager", "redis-cache"],
    "rootCause": "No jitter in client token-refresh retry logic; all sessions had nearly identical expiry times set during the previous auth-service startup.",
    "resolution": "Added randomised jitter (0–60 s) to token refresh scheduling; increased Redis connection pool limit on session-manager.",
    "resolvedInMin": 14,
    "tags": ["auth", "session", "redis", "thundering-herd", "token-refresh"]
  },
  {
    "id": "INC-009",
    "title": "Database connection pool exhausted — postgres-primary under peak load",
    "description": "trade-executor p99 latency exceeded 8 s during market open. DB connection pool saturated; new requests queued then timed out. A slow reporting query introduced in the prior deploy held connections for up to 45 seconds.",
    "severity": "HIGH",
    "affectedServices": ["trade-executor", "postgres-primary", "settlement-service"],
    "rootCause": "Slow SQL query (missing composite index on trade_date, instrument_id) saturated the connection pool at peak load.",
    "resolution": "Added composite index; tuned query planner statistics; increased pool max-connections; added slow-query alert at 2 s.",
    "resolvedInMin": 35,
    "tags": ["database", "connection-pool", "slow-query", "latency", "index"]
  },
  {
    "id": "INC-010",
    "title": "Postgres primary replication lag — replica divergence",
    "description": "Streaming replication lag between postgres-primary and postgres-replica grew to 4.8 GB over 90 minutes. Read replicas began serving stale position data. risk-engine read stale positions and computed incorrect exposure.",
    "severity": "HIGH",
    "affectedServices": ["postgres-primary", "postgres-replica", "risk-engine", "position-service"],
    "rootCause": "Long-running vacuum on a large table held an exclusive lock intermittently, blocking WAL apply on the replica.",
    "resolution": "Cancelled the blocking vacuum; replica caught up within 12 minutes; scheduled vacuums moved to off-hours maintenance window.",
    "resolvedInMin": 52,
    "tags": ["database", "replication", "postgres", "replica-lag", "vacuum"]
  },
  {
    "id": "INC-011",
    "title": "Database deadlock cascade — settlement concurrent batch jobs",
    "description": "Two concurrent settlement batch jobs acquired row locks in opposite order, producing a deadlock. Both transactions rolled back repeatedly, backing up the settlement queue to 2,400 pending items.",
    "severity": "HIGH",
    "affectedServices": ["settlement-service", "postgres-primary", "clearing-service"],
    "rootCause": "Settlement batch jobs acquired locks in non-deterministic order; concurrent jobs processing overlapping instrument sets created circular waits.",
    "resolution": "Enforced deterministic lock ordering (sorted by instrument ID); added deadlock-retry with exponential backoff.",
    "resolvedInMin": 25,
    "tags": ["database", "deadlock", "settlement", "batch", "locking"]
  },
  {
    "id": "INC-012",
    "title": "Postgres primary failover — prolonged replication split-brain",
    "description": "Primary DB host was terminated by infrastructure automation due to a health check misconfiguration. Automated failover promoted the replica but replication slots were not cleaned up, causing WAL bloat to fill the disk on the new primary.",
    "severity": "CRITICAL",
    "affectedServices": ["postgres-primary", "postgres-replica", "trade-executor", "settlement-service"],
    "rootCause": "Stale replication slots retained WAL segments after failover; disk filled to 100%, causing the new primary to halt writes.",
    "resolution": "Dropped stale replication slots; freed disk; services recovered; added replication slot monitoring alert.",
    "resolvedInMin": 48,
    "tags": ["database", "failover", "replication-slots", "disk-full", "postgres"]
  },
  {
    "id": "INC-013",
    "title": "Risk engine heap exhaustion — position cache memory leak",
    "description": "risk-engine pod restarted 3 times in 20 minutes. Heap grew steadily over 4 hours before OOM kill. GC pressure was visible 45 minutes before crash; heap profile showed expired positions not being evicted.",
    "severity": "HIGH",
    "affectedServices": ["risk-engine", "position-service"],
    "rootCause": "Memory leak in position cache: expired positions were inserted but the eviction TTL logic had an off-by-one error that prevented cleanup.",
    "resolution": "Fixed TTL eviction bug; set container memory limit and request; added heap-usage alert at 80% with automatic thread dump capture.",
    "resolvedInMin": 45,
    "tags": ["memory", "heap", "oom", "gc", "risk-engine", "memory-leak"]
  },
  {
    "id": "INC-014",
    "title": "GC pause storm — price-engine throughput collapse at market open",
    "description": "During peak market open, price-engine GC pause times exceeded 2.1 s, reducing effective throughput by 80%. Price updates lagged by 3–6 seconds, triggering stale-price rejections at order-gateway.",
    "severity": "HIGH",
    "affectedServices": ["price-engine", "market-data-feed", "order-gateway"],
    "rootCause": "Excessive short-lived object allocation in the price normalisation hot path triggered frequent full GC cycles under sustained 50k msg/s throughput.",
    "resolution": "Refactored hot path to use object pooling; switched to G1GC with region-size tuning; GC pauses dropped to under 50 ms.",
    "resolvedInMin": 60,
    "tags": ["memory", "gc", "performance", "price-engine", "throughput"]
  },
  {
    "id": "INC-015",
    "title": "Matching engine heap OOM — order book object accumulation",
    "description": "matching-engine reported heap at 97% and then was OOM-killed. A code change introduced in the prior release did not release completed order book entries; they accumulated over the trading session.",
    "severity": "CRITICAL",
    "affectedServices": ["matching-engine", "order-gateway", "trade-executor"],
    "rootCause": "Order book entries were added to an unbounded list but the cleanup routine was inadvertently removed in a refactor.",
    "resolution": "Rolled back the release to prior version; order book cleanup restored; added heap profile regression test to release gate.",
    "resolvedInMin": 20,
    "tags": ["memory", "heap", "oom", "matching-engine", "regression"]
  },
  {
    "id": "INC-016",
    "title": "GC pause causing order latency SLA breach — margin-service",
    "description": "margin-service p99 latency breached the 500 ms SLA during end-of-day margining. Investigation showed GC pauses averaging 700 ms every 90 seconds due to large object promotion in the old generation.",
    "severity": "MEDIUM",
    "affectedServices": ["margin-service", "risk-engine", "position-service"],
    "rootCause": "Large margin calculation result objects were promoted to old generation without being freed between sessions, causing major GC cycles.",
    "resolution": "Moved result objects to off-heap storage using pooled byte buffers; major GC frequency dropped by 95%.",
    "resolvedInMin": 90,
    "tags": ["memory", "gc", "margin-service", "latency", "sla-breach"]
  },
  {
    "id": "INC-017",
    "title": "Kafka consumer group rebalance storm — order events delayed",
    "description": "A rolling restart of the order-event consumer group triggered repeated rebalancing. 14 rebalance cycles occurred over 8 minutes, during which no consumers were assigned partitions. 9,000 order events accumulated in the topic with no processing.",
    "severity": "HIGH",
    "affectedServices": ["kafka-broker", "order-gateway", "trade-executor", "risk-engine"],
    "rootCause": "Consumer group session timeout was set to 10 seconds; pod restarts during rolling deploy exceeded the timeout causing repeated leader elections.",
    "resolution": "Increased session.timeout.ms to 60 s; set max.poll.interval.ms accordingly; staggered rolling restart cadence.",
    "resolvedInMin": 18,
    "tags": ["kafka", "consumer-group", "rebalance", "order-events", "messaging"]
  },
  {
    "id": "INC-018",
    "title": "Kafka DLQ overflow — poison pill trade confirmation messages",
    "description": "Trade confirmation consumer repeatedly failed on a batch of 240 malformed messages. After 3 retry attempts each, all 240 were routed to the dead-letter queue. DLQ consumer was not running, causing DLQ topic to hit retention limit.",
    "severity": "HIGH",
    "affectedServices": ["kafka-broker", "trade-executor", "settlement-service", "notification-service"],
    "rootCause": "A schema change in trade confirmation messages was not backward-compatible; legacy message format caused deserialization failure in all consumers.",
    "resolution": "Deployed schema-compatible consumer; replayed DLQ messages; added Avro schema registry compatibility check to producer CI.",
    "resolvedInMin": 55,
    "tags": ["kafka", "dlq", "poison-pill", "schema", "trade-confirmation"]
  },
  {
    "id": "INC-019",
    "title": "Kafka out-of-order event processing — position calculation errors",
    "description": "Position service processed trade events out of sequence after a partition reassignment. Positions for 3 accounts showed incorrect values. Downstream risk calculations used the incorrect positions for 12 minutes before detection.",
    "severity": "HIGH",
    "affectedServices": ["kafka-broker", "position-service", "risk-engine", "trade-executor"],
    "rootCause": "Partition reassignment caused two consumers to briefly process the same partition; duplicate and out-of-order delivery violated position calculation idempotency assumptions.",
    "resolution": "Added sequence-number validation and idempotency keys to position updates; implemented exactly-once semantics via Kafka transactions.",
    "resolvedInMin": 40,
    "tags": ["kafka", "ordering", "position", "idempotency", "partition"]
  },
  {
    "id": "INC-020",
    "title": "Kafka broker leader election — topic unavailability during ISR shrink",
    "description": "A Kafka broker in us-east-1c ran out of disk space, causing it to leave the ISR for all partitions it led. Leader elections for 48 partitions occurred simultaneously. Producers experienced up to 30 seconds of unavailability.",
    "severity": "CRITICAL",
    "affectedServices": ["kafka-broker", "zookeeper", "order-gateway", "trade-executor"],
    "rootCause": "Log retention was not configured for the high-throughput market-event topic; the broker disk filled to 100% within 6 hours of a traffic spike.",
    "resolution": "Added log.retention.bytes limit; cleared disk; broker rejoined ISR; added disk utilisation alert at 75%.",
    "resolvedInMin": 32,
    "tags": ["kafka", "broker", "leader-election", "disk-full", "isr"]
  },
  {
    "id": "INC-021",
    "title": "Kafka consumer lag — risk-engine falls 2M messages behind",
    "description": "risk-engine Kafka consumer lag grew to 2.1 million messages over 40 minutes. Consumer throughput dropped after a CPU throttle was incorrectly applied to the pod. Real-time risk calculations became stale by up to 8 minutes.",
    "severity": "HIGH",
    "affectedServices": ["kafka-broker", "risk-engine", "position-service"],
    "rootCause": "A Kubernetes LimitRange change set CPU limit to 0.5 cores on risk-engine pods; the consumer could not keep pace with the 50k msg/s topic rate.",
    "resolution": "Corrected CPU limit to 4 cores; consumer lag cleared in 18 minutes; added consumer-lag alert at 500k messages.",
    "resolvedInMin": 58,
    "tags": ["kafka", "consumer-lag", "cpu-throttle", "risk-engine", "kubernetes"]
  },
  {
    "id": "INC-022",
    "title": "Network partition — AZ-b isolated from AZ-a (cross-AZ traffic blocked)",
    "description": "AWS us-east-1b became unreachable from us-east-1a for 8 minutes. Services with pods in both AZs experienced connection timeouts. Service mesh reported high error rates on cross-AZ calls; health checks began failing.",
    "severity": "CRITICAL",
    "affectedServices": ["trade-executor", "risk-engine", "order-gateway", "settlement-service", "api-gateway"],
    "rootCause": "AWS network ACL rule change incorrectly blocked TCP traffic between VPC subnets in different AZs during a routine security audit update.",
    "resolution": "Reverted the ACL rule; rerouted traffic to healthy AZ; added cross-AZ connectivity health check with 30-second alerting.",
    "resolvedInMin": 15,
    "tags": ["network", "partition", "aws", "availability-zone", "service-mesh"]
  },
  {
    "id": "INC-023",
    "title": "Service mesh mTLS certificate expiry — inter-service calls blocked",
    "description": "Istio mTLS certificates expired simultaneously across 14 services due to a cert-manager misconfiguration. All inter-service gRPC calls returned TLS handshake errors. Services degraded to local cache operation.",
    "severity": "CRITICAL",
    "affectedServices": ["api-gateway", "trade-executor", "risk-engine", "order-gateway", "settlement-service"],
    "rootCause": "cert-manager renewal annotation was removed from the Istio CA secret during a Helm upgrade; certificates expired without automatic renewal.",
    "resolution": "Manually rotated certificates; restored cert-manager annotation; certificate rotation now tested in staging before production rollout.",
    "resolvedInMin": 28,
    "tags": ["network", "tls", "mtls", "certificate", "service-mesh", "istio"]
  },
  {
    "id": "INC-024",
    "title": "DNS resolution failure — service discovery breakdown",
    "description": "CoreDNS pods in the cluster experienced an OOM restart. During the 90-second restart window, service discovery via DNS failed for all inter-service calls. Services using IP-based discovery were unaffected.",
    "severity": "HIGH",
    "affectedServices": ["api-gateway", "auth-service", "trade-executor", "risk-engine"],
    "rootCause": "CoreDNS memory limit was set too low; a spike in DNS query rate from a misconfigured service caused CoreDNS to exceed its limit.",
    "resolution": "Increased CoreDNS memory limit; deployed 3 replicas for HA; added DNS query-rate monitoring.",
    "resolvedInMin": 6,
    "tags": ["network", "dns", "coredns", "service-discovery", "kubernetes"]
  },
  {
    "id": "INC-025",
    "title": "Load balancer connection draining timeout — rolling restart disruption",
    "description": "During a rolling restart, the load balancer did not drain connections before pods were terminated. In-flight trade requests received connection reset errors. 340 trades required manual status reconciliation.",
    "severity": "MEDIUM",
    "affectedServices": ["api-gateway", "trade-executor", "order-gateway"],
    "rootCause": "terminationGracePeriodSeconds was set to 5 s but in-flight request processing required up to 30 s; load balancer deregistered pods before they finished serving.",
    "resolution": "Increased terminationGracePeriodSeconds to 60 s; added preStop hook to delay pod termination by 30 s for connection draining.",
    "resolvedInMin": 12,
    "tags": ["network", "load-balancer", "connection-draining", "rolling-restart", "kubernetes"]
  },
  {
    "id": "INC-026",
    "title": "Circuit breaker open cascade — settlement-service timeout propagation",
    "description": "settlement-service latency spiked after a DB replica failover. Circuit breakers in trade-executor and clearing-service opened within 60 seconds. All settlement operations were rejected for 7 minutes while the breaker remained open.",
    "severity": "HIGH",
    "affectedServices": ["settlement-service", "trade-executor", "clearing-service", "order-gateway"],
    "rootCause": "DB replica failover increased settlement-service response time beyond the circuit breaker threshold of 2 s; no fallback was implemented for non-critical settlement lookups.",
    "resolution": "Added async fallback for non-critical settlement queries; increased circuit breaker timeout to 5 s with half-open probe every 30 s.",
    "resolvedInMin": 18,
    "tags": ["circuit-breaker", "settlement", "cascade", "resilience", "fallback"]
  },
  {
    "id": "INC-027",
    "title": "Circuit breaker flapping — risk-engine intermittent timeouts",
    "description": "risk-engine circuit breaker on the position-service dependency entered a rapid open/half-open/open cycle every 45 seconds for 25 minutes. Trade submission was intermittently blocked as risk checks were skipped.",
    "severity": "HIGH",
    "affectedServices": ["risk-engine", "position-service", "trade-executor"],
    "rootCause": "Position-service pod underwent GC pauses of 3–4 seconds every 45 seconds, just exceeding the 2-second timeout threshold and triggering the breaker on each cycle.",
    "resolution": "Tuned JVM heap on position-service to reduce GC frequency; increased circuit breaker timeout to 6 s; added half-open minimum probe count of 3.",
    "resolvedInMin": 30,
    "tags": ["circuit-breaker", "gc", "position-service", "flapping", "risk-engine"]
  },
  {
    "id": "INC-028",
    "title": "Circuit breaker open — compliance-engine bulk timeout",
    "description": "compliance-engine circuit breaker opened on the reporting-service dependency after 5 consecutive timeouts. Compliance checks for order submission were bypassed via fallback. Regulators were notified of a 14-minute gap in real-time compliance checking.",
    "severity": "HIGH",
    "affectedServices": ["compliance-engine", "reporting-service", "order-gateway", "audit-service"],
    "rootCause": "reporting-service was waiting on a long-running batch report that held all available DB connections; compliance queries timed out as a result.",
    "resolution": "Separated reporting and compliance DB connection pools; added read-replica routing for compliance queries.",
    "resolvedInMin": 22,
    "tags": ["circuit-breaker", "compliance", "reporting", "database", "connection-pool"]
  },
  {
    "id": "INC-029",
    "title": "Circuit breaker open — notification-service SMTP relay timeout",
    "description": "notification-service circuit breaker opened after the SMTP relay became unresponsive. Trade confirmation emails and SMS alerts were queued locally. Queue depth grew to 18,000 pending notifications over 45 minutes.",
    "severity": "MEDIUM",
    "affectedServices": ["notification-service", "trade-executor", "settlement-service"],
    "rootCause": "SMTP relay provider performed an unannounced maintenance window; notification-service had no secondary relay configured.",
    "resolution": "Added secondary SMTP relay as failover; circuit breaker now retries secondary on open state; notification backlog flushed within 8 minutes.",
    "resolvedInMin": 55,
    "tags": ["circuit-breaker", "notification", "smtp", "email", "queue"]
  },
  {
    "id": "INC-030",
    "title": "Rate limiter triggering globally — market data consumer retry flood",
    "description": "A misconfigured retry loop in market-data-consumer sent requests at full speed on 503 responses (no backoff), exhausting shared API gateway quota. 429 responses were returned to all clients including trading applications.",
    "severity": "MEDIUM",
    "affectedServices": ["api-gateway", "market-data-feed", "order-gateway"],
    "rootCause": "Retry logic missing exponential backoff; 503 from market-data-feed triggered infinite retry at full request rate, consuming the global quota.",
    "resolution": "Added exponential backoff with jitter to market-data-consumer retry; added per-client rate limit bucket at API gateway.",
    "resolvedInMin": 12,
    "tags": ["rate-limiting", "retry-storm", "api-gateway", "market-data", "backoff"]
  },
  {
    "id": "INC-031",
    "title": "Rate limiter misconfiguration — FIX clients blocked during high-volume session",
    "description": "A rate limiter configuration change accidentally applied an order-submission limit of 10 req/s to all FIX clients instead of 1000 req/s. Large institutional clients were throttled during a high-volume trading session.",
    "severity": "HIGH",
    "affectedServices": ["fix-gateway", "api-gateway", "order-gateway"],
    "rootCause": "Rate limit values were stored in the config-service as integers; a YAML serialisation issue converted 1000 to 10 during a config push.",
    "resolution": "Corrected config values; reloaded rate limiter without restart; added config diff review step for rate-limit changes.",
    "resolvedInMin": 9,
    "tags": ["rate-limiting", "fix-gateway", "config", "serialization", "institutional"]
  },
  {
    "id": "INC-032",
    "title": "Risk limit breach rejection storm — desk-level position limit exceeded",
    "description": "A high-frequency algo desk exceeded its intraday position limit for US equity options. risk-engine began rejecting all new orders from the desk. 1,200 orders were rejected in 4 minutes, causing the desk to contact the operations team.",
    "severity": "HIGH",
    "affectedServices": ["risk-engine", "order-gateway", "position-service", "compliance-engine"],
    "rootCause": "The desk's intraday limit was not updated after their mandate expansion; the old limit was 40% lower than the new authorised level.",
    "resolution": "Updated desk position limit in risk-engine config; reviewed and resubmitted rejected orders; implemented a limit-change approval workflow.",
    "resolvedInMin": 20,
    "tags": ["risk", "position-limit", "rejection", "equities", "business-error"]
  },
  {
    "id": "INC-033",
    "title": "FIX protocol session drop — market maker connectivity lost",
    "description": "fix-gateway dropped FIX session with primary market maker after a heartbeat timeout. The market maker's quotes were withdrawn from the order book. Spread widened significantly for 18 minutes before the session was re-established.",
    "severity": "HIGH",
    "affectedServices": ["fix-gateway", "order-gateway", "matching-engine"],
    "rootCause": "fix-gateway TCP keepalive was not enabled; a network device in the co-location facility silently dropped idle TCP connections after 60 seconds.",
    "resolution": "Enabled TCP keepalive with 30-second interval on fix-gateway; added FIX heartbeat monitoring alert; tested with all market makers.",
    "resolvedInMin": 18,
    "tags": ["fix-protocol", "session-drop", "market-maker", "connectivity", "tcp-keepalive"]
  },
  {
    "id": "INC-034",
    "title": "FIX logon sequence error — duplicate CompID causing session rejection",
    "description": "A new fix-gateway instance was deployed with a CompID that conflicted with an existing session. The FIX counterparty rejected logon for both sessions. The original market participant was disconnected for 6 minutes.",
    "severity": "MEDIUM",
    "affectedServices": ["fix-gateway", "order-gateway"],
    "rootCause": "CompID assignment was manual and the deployment runbook was not updated to reflect the new instance numbering scheme.",
    "resolution": "Assigned correct unique CompID; reconnected affected session; automated CompID assignment from a central registry.",
    "resolvedInMin": 14,
    "tags": ["fix-protocol", "compid", "session", "deployment", "configuration"]
  },
  {
    "id": "INC-035",
    "title": "Margin call processing failure — batch job timeout",
    "description": "End-of-day margin call processing job timed out after 4 hours, exceeding its 3-hour SLA. 847 accounts did not receive margin calls. Risk team had to manually trigger margin calls and notify clients.",
    "severity": "CRITICAL",
    "affectedServices": ["margin-service", "position-service", "risk-engine", "notification-service"],
    "rootCause": "Margin calculation for a large portfolio used an unoptimised scenario generation loop; adding a new stress scenario in the prior release increased computation time by 60%.",
    "resolution": "Parallelised scenario generation across 8 threads; optimised portfolio-level calculation; margin batch now completes in 90 minutes.",
    "resolvedInMin": 240,
    "tags": ["margin", "batch", "timeout", "performance", "business-error"]
  },
  {
    "id": "INC-036",
    "title": "Compliance rule engine timeout — pre-trade check SLA breach",
    "description": "compliance-engine pre-trade checks began timing out (>500 ms) for orders involving sanctions-screened counterparties. 3,400 orders were delayed beyond the 200 ms SLA. Two orders were auto-rejected due to timeout fallback policy.",
    "severity": "HIGH",
    "affectedServices": ["compliance-engine", "order-gateway", "audit-service"],
    "rootCause": "Sanctions list database was not indexed for partial-name matching; a regulatory update added 4,200 new entries, causing full-table scans on every pre-trade check.",
    "resolution": "Added trigram index for partial-name matching; sanctions list now cached in Redis with 5-minute TTL; pre-trade check latency returned to 12 ms.",
    "resolvedInMin": 75,
    "tags": ["compliance", "sanctions", "pre-trade", "latency", "database"]
  },
  {
    "id": "INC-037",
    "title": "Risk VaR calculation spike — incorrect historical data feed",
    "description": "risk-engine computed VaR values 3x normal for all portfolios due to incorrect historical volatility data injected by a faulty data pipeline. Several desks received erroneous margin top-up notifications.",
    "severity": "HIGH",
    "affectedServices": ["risk-engine", "market-data-feed", "margin-service", "notification-service"],
    "rootCause": "Historical volatility data pipeline ingested a corrupted file from the data vendor; no schema or range validation was applied at ingestion.",
    "resolution": "Rolled back to prior day's volatility data; recalculated VaR; retracted incorrect margin notifications; added range-check validation to data ingestion pipeline.",
    "resolvedInMin": 65,
    "tags": ["risk", "var", "data-quality", "volatility", "margin"]
  },
  {
    "id": "INC-038",
    "title": "Bad config push — config-service pushed null environment variables",
    "description": "A config-service deployment pushed a YAML file with null values for 12 critical environment variables. Dependent services (order-gateway, risk-engine) read empty strings and failed to connect to their dependencies.",
    "severity": "CRITICAL",
    "affectedServices": ["config-service", "order-gateway", "risk-engine", "trade-executor"],
    "rootCause": "YAML merge key behaviour produced null values for keys that were present in the base config but absent in the environment-specific overlay.",
    "resolution": "Rolled back config deployment; added null-value validation to config CI; deployed config diff preview tool for operators.",
    "resolvedInMin": 15,
    "tags": ["config", "deployment", "null-values", "yaml", "environment"]
  },
  {
    "id": "INC-039",
    "title": "Rolling deployment version mismatch — API contract breaking change",
    "description": "A rolling deployment of trade-executor introduced a breaking change in the internal trade submission API. Old pods (v1.8) and new pods (v1.9) ran simultaneously for 12 minutes. Calls routed to old pods failed when downstream services sent v1.9 payloads.",
    "severity": "HIGH",
    "affectedServices": ["trade-executor", "order-gateway", "settlement-service"],
    "rootCause": "API versioning policy was not enforced; the v1.9 response schema removed a field that old-version pods required, causing deserialization errors.",
    "resolution": "Paused deployment; rolled back to v1.8; added API compatibility tests to deployment gate; enforced additive-only schema changes policy.",
    "resolvedInMin": 22,
    "tags": ["deployment", "version-mismatch", "api-contract", "rolling-restart", "schema"]
  },
  {
    "id": "INC-040",
    "title": "Config-service unavailability — services unable to reload runtime config",
    "description": "config-service became unavailable for 35 minutes after an AWS EBS volume detachment. Services using dynamic config reload began operating with stale config. A risk limit change made during the outage was not propagated.",
    "severity": "MEDIUM",
    "affectedServices": ["config-service", "risk-engine", "order-gateway", "compliance-engine"],
    "rootCause": "Single-AZ EBS volume was detached during an AWS AZ maintenance event; config-service had no multi-AZ persistent storage.",
    "resolution": "Migrated config-service persistence to multi-AZ EFS; added config-service health check to deployment readiness gate.",
    "resolvedInMin": 40,
    "tags": ["config", "availability", "aws", "storage", "ebs"]
  },
  {
    "id": "INC-041",
    "title": "Order gateway matching engine disconnect — orders queuing without execution",
    "description": "order-gateway lost its gRPC connection to matching-engine after a network blip. Orders continued to be accepted and queued locally. 4,200 orders accumulated without execution for 6 minutes before the disconnect was detected.",
    "severity": "CRITICAL",
    "affectedServices": ["order-gateway", "matching-engine", "trade-executor"],
    "rootCause": "gRPC keepalive was not configured; the load balancer's idle connection timeout terminated the connection after 60 seconds of low traffic.",
    "resolution": "Configured gRPC keepalive (30 s ping interval); added real-time order processing rate monitoring; added automatic disconnect alert.",
    "resolvedInMin": 10,
    "tags": ["order-gateway", "matching-engine", "grpc", "connectivity", "keepalive"]
  },
  {
    "id": "INC-042",
    "title": "Matching engine order book corruption — sequence gap under failover",
    "description": "matching-engine primary failed over to standby during peak trading. A 3-second sequence gap in order events occurred during failover. Post-failover reconciliation found 12 orders in an inconsistent state between order-gateway and matching-engine.",
    "severity": "CRITICAL",
    "affectedServices": ["matching-engine", "order-gateway", "trade-executor", "audit-service"],
    "rootCause": "Standby matching-engine did not replay the in-flight event log from the primary before accepting new orders; sequence continuity was broken.",
    "resolution": "Implemented in-flight event log replay before standby activation; added post-failover reconciliation job that runs automatically.",
    "resolvedInMin": 35,
    "tags": ["matching-engine", "failover", "order-book", "sequence", "reconciliation"]
  },
  {
    "id": "INC-043",
    "title": "Order gateway rate limiting — algorithmic client exceeded TPS quota",
    "description": "An algorithmic client submitted orders at 4,200 TPS, exceeding their contractual 1,000 TPS quota. order-gateway rate limiter triggered correctly, but the client's retry logic caused a 12x amplification in connection attempts, overwhelming the gateway's connection pool.",
    "severity": "MEDIUM",
    "affectedServices": ["order-gateway", "api-gateway", "matching-engine"],
    "rootCause": "Client retry on 429 responses used a 100 ms fixed delay, amplifying connection load; connection pool on order-gateway was sized for expected TPS, not retry-amplified load.",
    "resolution": "Blocked client pending remediation; increased connection pool; added retry-amplification detection to rate limiter.",
    "resolvedInMin": 8,
    "tags": ["order-gateway", "rate-limiting", "algo-trading", "retry-amplification", "connection-pool"]
  },
  {
    "id": "INC-044",
    "title": "Settlement batch failure — clearing-service file format rejection",
    "description": "End-of-day settlement batch generated clearing files in SWIFT MT940 format but clearing-service expected ISO 20022 XML. All 1,847 settlement records were rejected. Overnight settlement was at risk.",
    "severity": "CRITICAL",
    "affectedServices": ["settlement-service", "clearing-service", "reporting-service"],
    "rootCause": "A configuration change to the settlement file format was applied only to the settlement-service without updating the corresponding parser in clearing-service.",
    "resolution": "Rolled back settlement-service file format to MT940; replayed batch; format migration now requires coordinated change approval.",
    "resolvedInMin": 90,
    "tags": ["settlement", "clearing", "file-format", "swift", "iso20022"]
  },
  {
    "id": "INC-045",
    "title": "Settlement service position reconciliation mismatch — day-end discrepancy",
    "description": "Day-end position reconciliation between settlement-service and position-service found 23 accounts with mismatched net positions. Discrepancy was traced to a 4-minute window during which Kafka consumer offset commit failures caused duplicate position updates.",
    "severity": "HIGH",
    "affectedServices": ["settlement-service", "position-service", "kafka-broker", "clearing-service"],
    "rootCause": "Kafka consumer committed offsets in a separate transaction from the position DB write; a broker connectivity blip caused offset commits to fail after positions were already written, leading to reprocessing and duplicates.",
    "resolution": "Implemented transactional outbox pattern for position updates; added idempotency keys on all position write operations; ran reconciliation job to correct the 23 accounts.",
    "resolvedInMin": 110,
    "tags": ["settlement", "reconciliation", "kafka", "idempotency", "position", "duplicate-processing"]
  }
]
